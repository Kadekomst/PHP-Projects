<?php

/*
 * Registration Database Config
*/

$host = '127.0.0.1';
$db_name = 'registration_login_pdo';
$username = 'root';
$password = '';
$charset = 'utf8';

$db_config = array(
    'host' => $host,
    'db_name' => $db_name,
    'username' => $username,
    'password' => $password,
    'charset' => $charset
);

return $db_config;